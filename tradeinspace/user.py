from flask.ext.login import LoginManager
from tradeinspace import app
# The User class for flask-login

login_manager = LoginManager()
login_manager.init_app(app)

class User()
    def get_id(x):
        return chr(x)

    is_authenticated = flask_login.login_required()
    is_active = True
    is_anonymous = False
    get_id(x)
